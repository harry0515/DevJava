1. Spring Boot starter parent
2. Spring Boot starter web
3. Java version 8
4. Spring Boot plugin
5. Create Spring Boot application launcher.

annotations @Override, @Deprecated, @SuppressWarings("unchecked"), @Functional Interface


spring boot annotations

@RestController
@Autowired
@RequestMapping
@Component

idempotent
// put updating existing source, edit, can done repeatedly
// get, can done repeatedly
// delete, can done repeatedly

non-idempotent
// post creating new source, add, cannot done repeatedly
